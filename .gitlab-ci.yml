image: maven:3-adoptopenjdk-11
stages:          
  - build
  - deploy

verify:
  stage: build
  script:
    - 'mvn  clean install'
    - 'mvn package'
  only:
    refs:
      - merge_requests



build-deploy-docker:
  stage: deploy
  image: docker:latest
  before_script:
      - docker login -u johnskyi2@yandex.ru --password-stdin 89taman89 registry.gitlab.com

  script:
    - docker build -t javapro-backend .
    - docker run -d -p 8080:8080 javapro-backend
  tags:
    - javapro
  only:
    refs:
      - main
  except:
    refs:
      - merge_requests

